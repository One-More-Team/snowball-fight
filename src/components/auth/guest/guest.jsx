import React from "react";

import styles from "./guest.module.scss";

const Guest = () => (
  <div className={styles.Wrapper}>
    Play as Guest<i class="fas fa-play-circle"></i>
  </div>
);
export default Guest;
